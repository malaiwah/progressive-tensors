# Development / CI inputs.  Compile with:
#   uv pip compile --universal requirements-dev.in -o requirements-dev.txt
# The locked output is what CI installs; edit this file, never the lock.
pytest>=8
pynacl>=1.5          # ed25519 signing + verification (runtime dependency)
numpy>=1.24          # fq_eps, fq_verify --similarity, test fixtures
jsonschema>=4.21     # schemas/ conformance tests
